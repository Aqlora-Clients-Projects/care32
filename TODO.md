- [ ] Add shared `.medilink-btn` class in `public/assets/dist/css/style.css` to standardize button width/height/padding and icon spacing.
- [ ] Update `app/Views/pages/Home.php` to apply `.medilink-btn` to:
  - [ ] Hero CTA link (`.hero-cta`)
  - [ ] Services header CTA (`Book an Appointment`)
  - [ ] Load More button
  - [ ] Confirm Appointment submit button
- [x] Verify visually in browser (hero + services/buttons) to confirm all buttons match dimensions.


